const classes = {
  root: "absolute bottom-0 w-full border-t border-t-gray-200 bg-white px-4 py-5 flex items-center justify-center",
};

const Footer = () => <footer className={classes.root}>Footer</footer>;

export default Footer;
